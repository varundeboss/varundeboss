1) Add only schema columns that are needed based on the initial json structure provided.
2) Make the return string for every model to be string instead of integer(PK)
3) Create a product to create schemas(django, mysql etc,.) from a json being uploaded